<%- include("partials/header") %>
<body>
	<div class="container-fluid">
		<div class="row">
			<div class="col-sm-2">
				<%- include("partials/sidebar") %>

			</div>
			<div class="col-sm-10">
				<h1><%= titre %></h1>
				<ul>
					<% if (liste_genre.length == 0) { %>
					<li> Aucun genre de livres.</li>
					<% } else { %>
					<% for ( let genre of liste_genre) { %>
					<li><a href= genre.url ><%= genre.genre %></a></li>
					<% } %>
					<% } %>
				</ul>
			</div>
		</div>
	</div>

</body>
</html>
